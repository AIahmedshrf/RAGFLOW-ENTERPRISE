# 🔐 دليل تسجيل الدخول إلى RAGFlow

## ❓ لا تحتاج إلى إنشاء حساب جديد (Sign Up)

RAGFlow يأتي **مع حساب افتراضي مُعد مسبقاً**. لا تحتاج إلى التسجيل!

---

## ✅ خطوات تسجيل الدخول

### 1️⃣ افتح المتصفح

اذهب إلى:
```
http://YOUR_SERVER_IP:8080
```

أو إذا كنت على نفس الخادم:
```
http://localhost:8080
```

### 2️⃣ ستظهر لك صفحة تسجيل الدخول

املأ الحقول التالية:

**البريد الإلكتروني:**
```
admin@ragflow.io
```

**كلمة المرور:**
```
admin
```

### 3️⃣ انقر "Sign In" أو "تسجيل الدخول"

سيتم تسجيل دخولك مباشرة إلى لوحة التحكم!

---

## 🔄 في حالة عدم وجود الحساب الافتراضي

إذا لم يعمل الحساب الافتراضي (نادراً جداً)، يمكنك:

### الخيار 1: التحقق من قاعدة البيانات

```bash
# الاتصال بـ MySQL
docker exec -it docker-mysql-1 mysql -uroot -pragflow_root_ChangeMe_!23 rag_flow

# التحقق من المستخدمين
SELECT email, status FROM user;
```

### الخيار 2: إنشاء مستخدم يدوياً (متقدم)

```bash
# الدخول إلى حاوية RAGFlow
docker exec -it docker-ragflow-cpu-1 bash

# تشغيل Python REPL
python3

# تنفيذ الكود التالي:
from api.db.services.user_service import UserService
UserService.save(
    email="youremail@example.com",
    password="YourPassword123",
    nickname="Your Name"
)
```

---

## 🔒 تغيير كلمة المرور بعد الدخول

### خطوات مهمة للأمان:

1. بعد تسجيل الدخول، اذهب إلى **Settings** أو **الإعدادات**
2. ابحث عن قسم **Account** أو **الحساب**
3. اختر **Change Password** أو **تغيير كلمة المرور**
4. أدخل:
   - كلمة المرور الحالية: `admin`
   - كلمة المرور الجديدة: (اختر كلمة مرور قوية)
   - تأكيد كلمة المرور الجديدة

---

## 🎯 ماذا تفعل بعد تسجيل الدخول؟

### 1. استكشف الواجهة
- **Dashboard:** نظرة عامة على النظام
- **Knowledge Base:** إدارة قواعد المعرفة
- **Chat:** إنشاء مساعدين للمحادثة
- **Settings:** الإعدادات والتكوين

### 2. إنشاء أول Knowledge Base
```
Knowledge Base → Create → أدخل الاسم → Create
```

### 3. رفع ملفات
```
افتح Knowledge Base → Upload Files → اختر الملفات → Upload
```

### 4. إنشاء Chat Assistant
```
Chat → Create Assistant → اختر Knowledge Base → اختر نموذج اللغة → Create
```

### 5. ابدأ المحادثة!
```
اطرح أسئلة حول المحتوى الذي رفعته
```

---

## 📸 لقطات شاشة توضيحية

### صفحة تسجيل الدخول ستبدو هكذا:

```
┌─────────────────────────────────┐
│         RAGFlow Logo            │
│                                 │
│  Email: [admin@ragflow.io    ] │
│  Password: [admin            ] │
│                                 │
│         [  Sign In  ]           │
│                                 │
└─────────────────────────────────┘
```

---

## ⚠️ ملاحظات مهمة

1. **لا تحتاج Sign Up:** الحساب موجود مسبقاً
2. **استخدم HTTPS في الإنتاج:** للأمان
3. **غيّر كلمة المرور:** فوراً بعد أول دخول
4. **احفظ البيانات:** لا تفقد كلمة المرور الجديدة

---

## 🆘 مشاكل تسجيل الدخول؟

### المشكلة: "Invalid credentials"

**الحل:**
1. تأكد من البريد الإلكتروني: `admin@ragflow.io`
2. تأكد من كلمة المرور: `admin`
3. لا تضع مسافات إضافية
4. تأكد من حالة الأحرف (lowercase)

### المشكلة: "Cannot connect to server"

**الحل:**
```bash
# تحقق من أن RAGFlow يعمل
docker logs docker-ragflow-cpu-1 --tail 50

# تحقق من المنفذ
curl http://localhost:8080
```

### المشكلة: صفحة بيضاء أو خطأ 500

**الحل:**
```bash
# أعد تشغيل RAGFlow
docker compose --profile cpu restart ragflow-cpu

# تحقق من السجلات
docker logs docker-ragflow-cpu-1 -f
```

---

## ✨ خلاصة سريعة

```
1. افتح: http://YOUR_SERVER_IP:8080
2. البريد: admin@ragflow.io
3. كلمة المرور: admin
4. انقر Sign In
5. ابدأ الاستخدام!
```

**لا تحتاج إلى Sign Up - الحساب جاهز! 🎉**

---

**تاريخ الإنشاء:** 2025-11-17  
**الإصدار:** RAGFlow v0.21.1-slim
